schemaVersion: 1
id: leo.puppet.analyze-browser-artifacts
name: analyze-browser-artifacts
version: 1.0.0
scope: puppet-node
domain: operation
category: collection
mode: assess
attack:
  tactics: [collection, credential-access]
platforms: [linux, windows, macos]
targets: [host, browser-profile, browser-artifact]
pack: host-postex
risk: medium
accessMode: read-only-sensitive
status: published
source: builtin
owner: leo
enabled: true
requires:
  tools: [getBasicInfo, exec, queryTask, stopTask, stageRemoteFileToWorkspace, queryRemoteFileStage, workspaceList, workspaceReadText, workspaceSearch, workspaceWriteText, workspaceExec, workspaceExecStatus, workspaceExecCancel, workspacePromote]
  skills: [recon-basic-info]
  facts: [target.authorized]
produces: [browser-artifact-manifest, browser-artifact-report, browser-timeline]
next: [hunt-credentials]
